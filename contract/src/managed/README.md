Generated by the Compact compiler. From `contract/`:

```bash
# Requires Compact CLI: https://github.com/midnightntwrk/compact
npm run compact
npm run build
```

This folder is intentionally empty in git until you run `compact compile …`.
